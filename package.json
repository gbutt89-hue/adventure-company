{
  "name": "adventure-company",
  "version": "0.3.4",
  "private": true,
  "description": "Seeded fantasy expedition company browser prototype",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --check engine.js && node --check game.js && node --check sw.js && node test-engine.js && node test-ui.js"
  },
  "engines": {
    "node": ">=20"
  }
}
